export const publicRoutes = ['/', '/faker'];

export const authRoutes = ['/login'];

export const adminRoot = '/control';

export const apiAuthPrefix = '/api/auth';

export const DEAFAULT_LOGIN_REDIRECT = '/';
